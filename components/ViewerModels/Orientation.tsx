import { useThree } from "@react-three/fiber";
import * as THREE from 'three'
export default () => {
  const { camera } = useThree()
  console.log(camera);
  // TODO: On Rotation Make A Cube That Rotates The Same Way
  // Return The Orientation Cube
  return (
    <></>
  )
}